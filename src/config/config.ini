[production]
hostname = localhost
username = 
password = 
database = torii

[staging]
hostname = localhost
username = root
password =
database = torii_dev

[development]
hostname = localhost
username = root
password =
database = torii_dev

[testing]
hostname = localhost
username = root
password = 
database = torii_testing
